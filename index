<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Proof of Funds Calculator</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Sora:wght@300;400;500;600;700&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
  <style>
    /* ─── DESIGN TOKENS ─────────────────────────────────── */
    :root {
      --radius-sm: 10px;
      --radius-md: 16px;
      --radius-lg: 24px;
      --radius-xl: 32px;

      --blue:    #4f8ef7;
      --blue-2:  #6fa3ff;
      --violet:  #8b5cf6;
      --teal:    #22d3a8;
      --amber:   #f59e0b;
      --red:     #ef4444;
      --orange:  #f97316;
      --green:   #10b981;

      --font-main: 'Sora', sans-serif;
      --font-mono: 'DM Mono', monospace;

      --transition: 220ms cubic-bezier(.4,0,.2,1);
      --shadow-card: 0 4px 24px rgba(0,0,0,.18);
      --shadow-glow: 0 0 32px rgba(79,142,247,.18);
    }

    /* ── DARK THEME (default) ── */
    [data-theme="dark"] {
      --bg:          #0d1117;
      --bg-2:        #161b24;
      --surface:     #1c2333;
      --surface-2:   #232d42;
      --border:      rgba(255,255,255,.08);
      --border-2:    rgba(255,255,255,.14);
      --text:        #e8edf5;
      --text-2:      #8b9ab5;
      --text-3:      #556279;
      --input-bg:    #111827;
      --input-border: rgba(255,255,255,.1);
    }

    /* ── LIGHT THEME ── */
    [data-theme="light"] {
      --bg:          #f0f4fb;
      --bg-2:        #e6ecf7;
      --surface:     #ffffff;
      --surface-2:   #f5f7fe;
      --border:      rgba(0,0,0,.08);
      --border-2:    rgba(0,0,0,.14);
      --text:        #111827;
      --text-2:      #4b5563;
      --text-3:      #9ca3af;
      --input-bg:    #f8faff;
      --input-border: rgba(0,0,0,.12);
      --shadow-card: 0 4px 24px rgba(0,0,0,.08);
    }

    /* ─── RESET & BASE ──────────────────────────────────── */
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: var(--font-main);
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
      transition: background var(--transition), color var(--transition);
      line-height: 1.5;
    }

    /* ─── LAYOUT ────────────────────────────────────────── */
    .app {
      max-width: 1080px;
      margin: 0 auto;
      padding: 24px 20px 48px;
    }

    /* ─── HEADER ────────────────────────────────────────── */
    header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 32px;
      flex-wrap: wrap;
      gap: 16px;
    }

    .header-left {
      display: flex;
      align-items: center;
      gap: 14px;
    }

    .logo-icon {
      width: 52px;
      height: 52px;
      border-radius: 14px;
      background: linear-gradient(135deg, #2c4ecb 0%, #5b21b6 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 22px;
      box-shadow: 0 4px 16px rgba(79,142,247,.35);
    }

    .header-titles h1 {
      font-size: 1.5rem;
      font-weight: 700;
      letter-spacing: -.02em;
      color: var(--text);
    }

    .header-titles p {
      font-size: .82rem;
      color: var(--text-2);
      margin-top: 2px;
    }

    .header-right {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .badge {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius-sm);
      padding: 8px 14px;
      font-size: .78rem;
      color: var(--text-2);
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .badge svg { color: var(--blue); flex-shrink: 0; }

    /* ─── THEME TOGGLE ──────────────────────────────────── */
    .theme-toggle {
      width: 52px;
      height: 28px;
      border-radius: 999px;
      background: var(--surface-2);
      border: 1px solid var(--border-2);
      cursor: pointer;
      position: relative;
      display: flex;
      align-items: center;
      padding: 3px;
      transition: background var(--transition);
      flex-shrink: 0;
    }

    .theme-toggle-knob {
      width: 22px;
      height: 22px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--blue), var(--violet));
      box-shadow: 0 2px 6px rgba(0,0,0,.3);
      transition: transform var(--transition);
      display: flex;
      align-items: center;
      justify-content: center;
    }

    [data-theme="light"] .theme-toggle-knob {
      transform: translateX(24px);
    }

    .theme-toggle-knob svg {
      width: 12px;
      height: 12px;
      fill: #fff;
      stroke: none;
    }

    /* ─── MAIN GRID ─────────────────────────────────────── */
    .main-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
      margin-bottom: 16px;
    }

    @media (max-width: 700px) {
      .main-grid { grid-template-columns: 1fr; }
    }

    /* ─── CARD ──────────────────────────────────────────── */
    .card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius-lg);
      padding: 24px;
      box-shadow: var(--shadow-card);
      transition: background var(--transition), border-color var(--transition), box-shadow var(--transition);
    }

    .card-header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 22px;
    }

    .card-icon {
      width: 36px;
      height: 36px;
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 16px;
      flex-shrink: 0;
    }

    .card-icon.blue  { background: rgba(79,142,247,.15); }
    .card-icon.teal  { background: rgba(34,211,168,.15); }
    .card-icon.violet{ background: rgba(139,92,246,.15); }

    .card-title {
      font-size: 1rem;
      font-weight: 600;
      color: var(--text);
    }

    /* ─── INPUT FIELD ───────────────────────────────────── */
    .field {
      margin-bottom: 14px;
    }

    .field label {
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: .78rem;
      font-weight: 500;
      color: var(--text-2);
      margin-bottom: 6px;
    }

    .field label .field-icon {
      width: 24px;
      height: 24px;
      border-radius: 6px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 12px;
    }

    .field label .field-icon.blue   { background: rgba(79,142,247,.18); color: var(--blue-2); }
    .field label .field-icon.green  { background: rgba(16,185,129,.18); color: var(--teal); }
    .field label .field-icon.orange { background: rgba(249,115,22,.18);  color: var(--orange); }
    .field label .field-icon.amber  { background: rgba(245,158,11,.18);  color: var(--amber); }

    .field input,
    .field select {
      width: 100%;
      background: var(--input-bg);
      border: 1px solid var(--input-border);
      border-radius: var(--radius-sm);
      padding: 11px 14px;
      font-family: var(--font-main);
      font-size: .88rem;
      color: var(--text);
      outline: none;
      transition: border-color var(--transition), box-shadow var(--transition), background var(--transition);
      appearance: none;
      -webkit-appearance: none;
    }

    .field input::placeholder { color: var(--text-3); }

    .field input:focus,
    .field select:focus {
      border-color: var(--blue);
      box-shadow: 0 0 0 3px rgba(79,142,247,.2);
    }

    .select-wrapper {
      position: relative;
    }

    .select-wrapper::after {
      content: '';
      position: absolute;
      right: 14px;
      top: 50%;
      transform: translateY(-50%);
      width: 10px;
      height: 6px;
      background: var(--text-3);
      clip-path: polygon(0 0, 100% 0, 50% 100%);
      pointer-events: none;
    }

    .select-wrapper select { padding-right: 36px; }

    /* ─── CTA BUTTON ────────────────────────────────────── */
    .btn-primary {
      width: 100%;
      padding: 14px;
      border: none;
      border-radius: var(--radius-sm);
      background: linear-gradient(135deg, #3b6ee8 0%, #7c3aed 100%);
      color: #fff;
      font-family: var(--font-main);
      font-size: .92rem;
      font-weight: 600;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 9px;
      transition: opacity var(--transition), transform 100ms;
      margin-top: 18px;
      letter-spacing: .01em;
      box-shadow: 0 4px 20px rgba(79,142,247,.3);
    }

    .btn-primary:hover  { opacity: .92; }
    .btn-primary:active { transform: scale(.98); }

    /* ─── SUMMARY PANEL ─────────────────────────────────── */
    .summary-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
      margin-bottom: 14px;
    }

    .stat-card {
      background: var(--surface-2);
      border: 1px solid var(--border);
      border-radius: var(--radius-md);
      padding: 14px 16px;
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      transition: background var(--transition);
    }

    .stat-label {
      font-size: .74rem;
      color: var(--text-2);
      margin-bottom: 6px;
      font-weight: 500;
    }

    .stat-value {
      font-size: 1.35rem;
      font-weight: 700;
      font-family: var(--font-mono);
      letter-spacing: -.02em;
    }

    .stat-value.blue   { color: var(--blue-2); }
    .stat-value.teal   { color: var(--teal); }
    .stat-value.violet { color: var(--violet); }
    .stat-value.amber  { color: var(--amber); }

    .stat-icon {
      width: 34px;
      height: 34px;
      border-radius: 9px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 15px;
      flex-shrink: 0;
    }

    .stat-icon.blue   { background: rgba(79,142,247,.15); }
    .stat-icon.teal   { background: rgba(34,211,168,.15); }
    .stat-icon.violet { background: rgba(139,92,246,.15); }
    .stat-icon.amber  { background: rgba(245,158,11,.15); }

    /* ─── HERO AMOUNT ───────────────────────────────────── */
    .hero-amount {
      background: linear-gradient(135deg, rgba(59,110,232,.15) 0%, rgba(124,58,237,.15) 100%);
      border: 1px solid rgba(79,142,247,.25);
      border-radius: var(--radius-md);
      padding: 20px 20px 16px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
    }

    .hero-amount-left {}

    .hero-amount-label {
      font-size: .76rem;
      color: var(--text-2);
      margin-bottom: 4px;
      font-weight: 500;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .hero-number {
      font-size: 2.2rem;
      font-weight: 700;
      font-family: var(--font-mono);
      color: var(--text);
      letter-spacing: -.03em;
    }

    .doc-icon {
      width: 52px;
      height: 52px;
      border-radius: 14px;
      background: linear-gradient(135deg, #3b6ee8 0%, #7c3aed 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 20px;
      flex-shrink: 0;
      box-shadow: 0 4px 14px rgba(79,142,247,.3);
    }

    /* ─── ACTION BUTTONS (result) ───────────────────────── */
    .action-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 10px;
      margin-top: 12px;
    }

    .btn-action {
      padding: 11px;
      border: 1px solid var(--border-2);
      border-radius: var(--radius-sm);
      background: var(--surface-2);
      color: var(--text);
      font-family: var(--font-main);
      font-size: .82rem;
      font-weight: 500;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 7px;
      transition: background var(--transition), border-color var(--transition), opacity var(--transition);
    }

    .btn-action:hover { border-color: var(--blue); color: var(--blue); }

    .btn-action.primary {
      background: var(--blue);
      border-color: var(--blue);
      color: #fff;
    }

    .btn-action.primary:hover { opacity: .88; }

    .secure-note {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      margin-top: 10px;
      font-size: .72rem;
      color: var(--text-3);
    }

    /* ─── EMPTY/INITIAL STATE ───────────────────────────── */
    .summary-empty {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 10px;
      min-height: 200px;
      color: var(--text-3);
      font-size: .85rem;
      text-align: center;
    }

    .summary-empty .empty-icon {
      font-size: 2.5rem;
      opacity: .5;
    }

    /* ─── BANK STATEMENT VALIDATOR ──────────────────────── */
    .validator-card {
      grid-column: 1 / -1;
    }

    .date-row {
      display: grid;
      grid-template-columns: 1fr auto 1fr auto;
      gap: 12px;
      align-items: end;
      flex-wrap: wrap;
    }

    @media (max-width: 560px) {
      .date-row {
        grid-template-columns: 1fr 1fr;
      }
      .arrow-divider { display: none; }
      .btn-date { grid-column: 1 / -1; }
    }

    .arrow-divider {
      padding-bottom: 11px;
      color: var(--text-3);
      font-size: .9rem;
    }

    .btn-date {
      padding: 11px 20px;
      border: none;
      border-radius: var(--radius-sm);
      background: linear-gradient(135deg, #2563eb, #7c3aed);
      color: #fff;
      font-family: var(--font-main);
      font-size: .84rem;
      font-weight: 600;
      cursor: pointer;
      white-space: nowrap;
      display: flex;
      align-items: center;
      gap: 8px;
      transition: opacity var(--transition), transform 100ms;
      box-shadow: 0 4px 14px rgba(79,142,247,.25);
    }

    .btn-date:hover  { opacity: .9; }
    .btn-date:active { transform: scale(.97); }

    /* ─── TIMELINE TRACK ────────────────────────────────── */
    .timeline {
      margin-top: 18px;
      position: relative;
    }

    .timeline-track {
      height: 4px;
      border-radius: 99px;
      background: var(--surface-2);
      position: relative;
      margin: 0 0 10px;
    }

    .timeline-fill {
      height: 100%;
      border-radius: 99px;
      background: linear-gradient(90deg, var(--blue), var(--teal));
      transition: width .4s cubic-bezier(.4,0,.2,1);
      width: 0%;
    }

    .timeline-dot-start,
    .timeline-dot-end {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: var(--teal);
      position: absolute;
      top: -3px;
    }

    .timeline-dot-start { left: 0; }
    .timeline-dot-end   { right: 0; }

    .timeline-labels {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .timeline-label-text {
      font-size: .72rem;
      color: var(--text-3);
      font-family: var(--font-mono);
    }

    .timeline-badge {
      font-size: .8rem;
      font-weight: 700;
      padding: 4px 14px;
      border-radius: 999px;
      font-family: var(--font-mono);
    }

    .timeline-badge.green  { background: rgba(16,185,129,.2);  color: var(--teal); }
    .timeline-badge.red    { background: rgba(239,68,68,.2);   color: var(--red); }
    .timeline-badge.orange { background: rgba(249,115,22,.2);  color: var(--orange); }

    /* ─── RESULT PANEL (validator) ──────────────────────── */
    .validator-result {
      background: var(--surface-2);
      border: 1px solid var(--border);
      border-radius: var(--radius-md);
      padding: 18px 20px;
      display: flex;
      align-items: center;
      gap: 16px;
      margin-top: 16px;
      transition: background var(--transition), border-color .3s;
    }

    .result-check {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 22px;
      flex-shrink: 0;
    }

    .result-check.green  { background: rgba(16,185,129,.18); }
    .result-check.red    { background: rgba(239,68,68,.18); }
    .result-check.orange { background: rgba(249,115,22,.18); }

    .result-text-label {
      font-size: .8rem;
      color: var(--text-2);
      margin-bottom: 3px;
    }

    .result-text-main {
      font-size: 1.5rem;
      font-weight: 700;
      font-family: var(--font-mono);
    }

    .result-text-main.green  { color: var(--teal); }
    .result-text-main.red    { color: var(--red); }
    .result-text-main.orange { color: var(--orange); }

    .result-text-sub {
      font-size: .74rem;
      color: var(--text-3);
      margin-top: 2px;
    }

    /* ─── FOOTER ────────────────────────────────────────── */
    footer {
      text-align: center;
      margin-top: 36px;
      font-size: .75rem;
      color: var(--text-3);
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 7px;
    }

    footer span { color: var(--violet); }

    /* ─── ANIMATIONS ────────────────────────────────────── */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(12px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    .result-appear {
      animation: fadeUp .3s ease both;
    }

    /* ─── TOAST ─────────────────────────────────────────── */
    .toast {
      position: fixed;
      bottom: 24px;
      left: 50%;
      transform: translateX(-50%) translateY(80px);
      background: var(--surface);
      border: 1px solid var(--border-2);
      border-radius: var(--radius-sm);
      padding: 10px 20px;
      font-size: .82rem;
      color: var(--text);
      box-shadow: var(--shadow-card);
      transition: transform .3s cubic-bezier(.4,0,.2,1), opacity .3s;
      opacity: 0;
      z-index: 999;
      white-space: nowrap;
    }

    .toast.show {
      transform: translateX(-50%) translateY(0);
      opacity: 1;
    }

    /* ─── SYSTEM PREFERENCE FLASH PREVENTION ────────────── */
    html { visibility: visible; }
  </style>
</head>
<body>
<div class="app">

  <!-- HEADER -->
  <header>
    <div class="header-left">
      <div class="logo-icon">£</div>
      <div class="header-titles">
        <h1>Proof of Funds Calculator</h1>
        <p>Calculate your UK student Proof of Funds instantly</p>
      </div>
    </div>
    <div class="header-right">
      <div class="badge">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
        <span>Accurate. Simple. Reliable.<br>Built for UK Student Visa Applications</span>
      </div>
      <button class="theme-toggle" onclick="toggleTheme()" aria-label="Toggle theme" title="Toggle light/dark mode">
        <div class="theme-toggle-knob" id="themeKnob">
          <svg id="themeIcon" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
            <!-- Moon (dark mode) -->
            <path d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z"/>
          </svg>
        </div>
      </button>
    </div>
  </header>

  <!-- MAIN GRID: Calculator + Summary -->
  <div class="main-grid">

    <!-- CALCULATOR CARD -->
    <div class="card">
      <div class="card-header">
        <div class="card-icon blue">🔢</div>
        <div class="card-title">Calculator</div>
      </div>

      <div class="field">
        <label>
          <span class="field-icon blue">💼</span>
          Total Tuition Fee (£)
        </label>
        <input type="number" id="tuition" placeholder="e.g. 12500" min="0">
      </div>

      <div class="field">
        <label>
          <span class="field-icon green">💳</span>
          Amount Already Paid (£)
        </label>
        <input type="number" id="paid" placeholder="e.g. 4000" min="0">
      </div>

      <div class="field">
        <label>
          <span class="field-icon orange">📍</span>
          Campus Location
        </label>
        <div class="select-wrapper">
          <select id="location">
            <option value="london">London (£13,347)</option>
            <option value="outside">Outside London (£10,234)</option>
          </select>
        </div>
      </div>

      <div class="field">
        <label>
          <span class="field-icon amber">📊</span>
          Percentage Required (1–5%)
        </label>
        <div class="select-wrapper">
          <select id="percentage">
            <option value="1">1%</option>
            <option value="2">2%</option>
            <option value="3" selected>3%</option>
            <option value="4">4%</option>
            <option value="5">5%</option>
          </select>
        </div>
      </div>

      <button class="btn-primary" onclick="calculate()">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><line x1="9" y1="9" x2="15" y2="9"/><line x1="9" y1="12" x2="15" y2="12"/><line x1="9" y1="15" x2="12" y2="15"/></svg>
        Calculate POF
      </button>
    </div>

    <!-- SUMMARY CARD -->
    <div class="card" id="summaryCard">
      <div class="card-header">
        <div class="card-icon teal">📈</div>
        <div class="card-title">Your POF Summary</div>
      </div>

      <!-- Empty state -->
      <div class="summary-empty" id="emptyState">
        <div class="empty-icon">📋</div>
        <div>Fill in the calculator and press<br><strong>Calculate POF</strong> to see your summary.</div>
      </div>

      <!-- Result (hidden until calc) -->
      <div id="resultState" style="display:none">
        <div class="summary-grid">
          <div class="stat-card">
            <div>
              <div class="stat-label">Tuition Fee Balance</div>
              <div class="stat-value blue" id="rBalance">£0.00</div>
            </div>
            <div class="stat-icon blue">💼</div>
          </div>
          <div class="stat-card">
            <div>
              <div class="stat-label">Living Expenses</div>
              <div class="stat-value teal" id="rLiving">£0.00</div>
            </div>
            <div class="stat-icon teal">🏠</div>
          </div>
          <div class="stat-card">
            <div>
              <div class="stat-label">Total POF Needed</div>
              <div class="stat-value violet" id="rTotal">£0.00</div>
            </div>
            <div class="stat-icon violet">💰</div>
          </div>
          <div class="stat-card">
            <div>
              <div class="stat-label" id="rPctLabel">3% of POF</div>
              <div class="stat-value amber" id="rPercent">£0.00</div>
            </div>
            <div class="stat-icon amber">%</div>
          </div>
        </div>

        <div class="hero-amount">
          <div class="hero-amount-left">
            <div class="hero-amount-label">
              <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
              Amount You Need to Show
            </div>
            <div class="hero-number" id="rHeroAmount">£0.00</div>
          </div>
          <div class="doc-icon">📄</div>
        </div>

        <div class="action-row">
          <button class="btn-action" onclick="copyResult()">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1"/></svg>
            Copy Result
          </button>
          <button class="btn-action primary" onclick="convertToNaira()">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg>
            Convert to Naira
          </button>
        </div>

        <div class="secure-note">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
          All calculations are secure and private
        </div>
      </div>
    </div>
  </div>

  <!-- BANK STATEMENT VALIDATOR -->
  <div class="card validator-card">
    <div class="card-header">
      <div class="card-icon violet">🗓️</div>
      <div class="card-title">Bank Statement Validator</div>
    </div>
    <p style="font-size:.8rem;color:var(--text-2);margin-top:-10px;margin-bottom:18px">Check if your bank statement duration meets UKVI requirements (minimum 28 days)</p>

    <div class="date-row">
      <div class="field" style="margin:0">
        <label>Start Date</label>
        <input type="date" id="startDate">
      </div>
      <div class="arrow-divider">→</div>
      <div class="field" style="margin:0">
        <label>End Date</label>
        <input type="date" id="endDate">
      </div>
      <div>
        <button class="btn-date" onclick="checkDates()">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
          Calculate Days
        </button>
      </div>
    </div>

    <!-- Timeline (hidden until calc) -->
    <div class="timeline" id="timeline" style="display:none">
      <div class="timeline-track">
        <div class="timeline-fill" id="timelineFill"></div>
        <div class="timeline-dot-start"></div>
        <div class="timeline-dot-end"></div>
      </div>
      <div class="timeline-labels">
        <span class="timeline-label-text" id="tlStart">—</span>
        <span class="timeline-badge" id="tlBadge"></span>
        <span class="timeline-label-text" id="tlEnd">—</span>
      </div>
    </div>

    <!-- Validator result -->
    <div class="validator-result" id="validatorResult" style="display:none">
      <div class="result-check" id="validatorIcon">✅</div>
      <div>
        <div class="result-text-label" id="validatorLabel">Statement is Usable</div>
        <div class="result-text-main" id="validatorDays">31 Days</div>
        <div class="result-text-sub">Minimum requirement: 28 days</div>
      </div>
    </div>
  </div>

  <footer>
    <span>♥</span> Built to help international students succeed
  </footer>
</div>

<!-- TOAST -->
<div class="toast" id="toast"></div>

<script>
  /* ─── THEME SYSTEM ─────────────────────────────────────── */
  const MOON_SVG = `<path d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z"/>`;
  const SUN_SVG  = `<circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>`;

  function setTheme(theme) {
    document.documentElement.setAttribute('data-theme', theme);
    localStorage.setItem('pof-theme', theme);
    const icon = document.getElementById('themeIcon');
    icon.innerHTML = theme === 'dark' ? MOON_SVG : SUN_SVG;
    if (theme === 'light') {
      icon.setAttribute('stroke', '#fff');
      icon.setAttribute('fill', 'none');
      icon.setAttribute('stroke-width', '2');
      icon.setAttribute('stroke-linecap', 'round');
    } else {
      icon.setAttribute('fill', '#fff');
      icon.setAttribute('stroke', 'none');
    }
  }

  function toggleTheme() {
    const cur = document.documentElement.getAttribute('data-theme');
    setTheme(cur === 'dark' ? 'light' : 'dark');
  }

  // Init: respect localStorage, else respect system preference
  (function initTheme() {
    const saved = localStorage.getItem('pof-theme');
    if (saved) { setTheme(saved); return; }
    const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    setTheme(prefersDark ? 'dark' : 'light');
  })();

  // Keep in sync with system if user has no preference saved
  window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', e => {
    if (!localStorage.getItem('pof-theme')) setTheme(e.matches ? 'dark' : 'light');
  });

  /* ─── HELPERS ──────────────────────────────────────────── */
  function fmt(n) {
    return '£' + n.toFixed(2).replace(/\B(?=(\d{3})+(?!\d))/g, ',');
  }

  function showToast(msg) {
    const t = document.getElementById('toast');
    t.textContent = msg;
    t.classList.add('show');
    setTimeout(() => t.classList.remove('show'), 2400);
  }

  /* ─── POF CALCULATOR ───────────────────────────────────── */
  let lastFinalAmount = 0;

  function calculate() {
    const tuition    = parseFloat(document.getElementById('tuition').value)    || 0;
    const paid       = parseFloat(document.getElementById('paid').value)       || 0;
    const location   = document.getElementById('location').value;
    const percentage = parseFloat(document.getElementById('percentage').value) || 3;

    const balance     = tuition - paid;
    const livingCost  = location === 'london' ? 13347 : 10234;
    const totalPof    = balance + livingCost;
    const finalAmount = totalPof * (percentage / 100);
    lastFinalAmount   = finalAmount;

    document.getElementById('rBalance').textContent  = fmt(balance);
    document.getElementById('rLiving').textContent   = fmt(livingCost);
    document.getElementById('rTotal').textContent    = fmt(totalPof);
    document.getElementById('rPctLabel').textContent = `${percentage}% of POF`;
    document.getElementById('rPercent').textContent  = fmt(finalAmount);
    document.getElementById('rHeroAmount').textContent = fmt(finalAmount);

    document.getElementById('emptyState').style.display  = 'none';
    const rs = document.getElementById('resultState');
    rs.style.display = 'block';
    rs.classList.remove('result-appear');
    void rs.offsetWidth; // reflow
    rs.classList.add('result-appear');
  }

  function copyResult() {
    const lines = [
      'POF Calculator Result',
      '─────────────────────',
      `Tuition Fee Balance : ${document.getElementById('rBalance').textContent}`,
      `Living Expenses     : ${document.getElementById('rLiving').textContent}`,
      `Total POF Needed    : ${document.getElementById('rTotal').textContent}`,
      `${document.getElementById('rPctLabel').textContent}          : ${document.getElementById('rPercent').textContent}`,
      '',
      `Amount You Need to Show: ${document.getElementById('rHeroAmount').textContent}`,
    ].join('\n');

    navigator.clipboard.writeText(lines)
      .then(() => showToast('✅ Copied to clipboard!'))
      .catch(() => showToast('⚠️ Could not copy — try manually.'));
  }

  function convertToNaira() {
    const amount = lastFinalAmount.toFixed(2);
    window.open(`https://www.oanda.com/currency-converter/en/?from=GBP&to=NGN&amount=${amount}`, '_blank');
  }

  /* ─── BANK STATEMENT VALIDATOR ─────────────────────────── */
  function checkDates() {
    const s = document.getElementById('startDate').value;
    const e = document.getElementById('endDate').value;

    if (!s || !e) { showToast('⚠️ Please select both dates.'); return; }

    const start = new Date(s); start.setHours(0,0,0,0);
    const end   = new Date(e); end.setHours(0,0,0,0);

    if (start > end) { showToast('⚠️ End date must be after start date.'); return; }

    const days = Math.round(Math.abs(end - start) / 86400000);

    // Timeline
    const fill = document.getElementById('timelineFill');
    const pct  = Math.min((days / 90) * 100, 100);
    document.getElementById('timeline').style.display = 'block';
    setTimeout(() => { fill.style.width = pct + '%'; }, 50);

    const fmtDate = d => d.toLocaleDateString('en-GB', { day:'2-digit', month:'short', year:'numeric' });
    document.getElementById('tlStart').textContent = fmtDate(start);
    document.getElementById('tlEnd').textContent   = fmtDate(end);

    const badge = document.getElementById('tlBadge');

    let iconClass, iconEmoji, labelText, mainClass, mainText;

    if (days < 28) {
      iconClass = 'red'; iconEmoji = '❌';
      labelText = 'Statement is Unusable';
      mainClass = 'red'; mainText = `${days} Days`;
      badge.textContent = `${days} Days`;
      badge.className = 'timeline-badge red';
    } else if (days === 28) {
      iconClass = 'orange'; iconEmoji = '⚠️';
      labelText = 'Statement is Risky';
      mainClass = 'orange'; mainText = `${days} Days`;
      badge.textContent = `${days} Days`;
      badge.className = 'timeline-badge orange';
    } else {
      iconClass = 'green'; iconEmoji = '✅';
      labelText = days >= 30 ? 'Statement is Great' : 'Statement is Usable';
      mainClass = 'green'; mainText = `${days} Days`;
      badge.textContent = `${days} Days`;
      badge.className = 'timeline-badge green';
    }

    const vr = document.getElementById('validatorResult');
    document.getElementById('validatorIcon').className = `result-check ${iconClass}`;
    document.getElementById('validatorIcon').textContent = iconEmoji;
    document.getElementById('validatorLabel').textContent = labelText;
    document.getElementById('validatorDays').className = `result-text-main ${mainClass}`;
    document.getElementById('validatorDays').textContent = mainText;

    vr.style.display = 'flex';
    vr.classList.remove('result-appear');
    void vr.offsetWidth;
    vr.classList.add('result-appear');
  }
</script>
</body>
</html>
